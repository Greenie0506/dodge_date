  development:
    adapter: postgresql
    database: dodge_date_dev
    username: pivotal
    password:
    host: localhost


  # Warning: The database defined as 'test' will be erased and
  # re-generated from your development database when you run 'rake'.
  # Do not set this db to the same as development or production.
  test:
    adapter: postgresql
    database: dodge_date_test
    username: pivotal
    password:
    host: localhost


  production:
    adapter: postgresql
    database: dodge_date_production
    username: pivotal
    password:
    host: localhost


